# Food Calories Estimation API

API для распознавания еды и расчета калорий/БЖУ по фотографии с использованием машинного обучения.

## Особенности

- 🍕 Распознавание блюд с помощью модели Food-101
- 📊 Оценка калорий и БЖУ (белки, жиры, углеводы)
- 📏 Оценка размера порции (вес и объем)
- 🤖 Интеграция с ChatGPT Vision для сбора данных
- 🎓 Возможность дообучения собственной модели
- 🗄️ Сохранение всех запросов в PostgreSQL
- 📸 Сохранение загруженных изображений

## Технологии

- **FastAPI** - современный веб-фреймворк
- **PostgreSQL** - база данных
- **PyTorch** - машинное обучение
- **Transformers** - предобученные модели (Food-101)
- **Alembic** - миграции базы данных
- **OpenCV** - обработка изображений
- **Docker** - для PostgreSQL (приложение запускается локально)

## Требования

- Python 3.11+
- PostgreSQL (через Docker или локально)
- ~2GB свободного места (для моделей ML)

## Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone <your-repo-url>
cd foodEstimation
```

### 2. Создайте виртуальное окружение

```bash
# Windows
python -m venv venv
.\venv\Scripts\Activate.ps1

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте окружение

Создайте файл `.env`:
```env
API_TOKEN=your-super-secret-token-here
DATABASE_URL=postgresql://user:password@localhost:5432/foodestimation
```

### 5. Запустите PostgreSQL

```bash
# Через Docker (рекомендуется)
docker-compose up -d
```

Или установите PostgreSQL локально и создайте базу данных.

### 6. Примените миграции

```bash
alembic upgrade head
```

### 7. Запустите приложение

```bash
uvicorn app.main:app --reload
```

API будет доступен по адресу: http://localhost:8000

Документация API: http://localhost:8000/docs

## Подробная документация

- [QUICKSTART.md](QUICKSTART.md) - пошаговая инструкция по запуску
- [MIGRATIONS.md](MIGRATIONS.md) - работа с миграциями базы данных
- [TRAINING.md](TRAINING.md) - обучение собственной модели
- [PROXY_SETUP.md](PROXY_SETUP.md) - настройка прокси для OpenAI API (если требуется)

## API Endpoints

### POST /recognize-food

Загрузить фото и получить информацию о калориях и БЖУ.

**Требует аутентификации:** Bearer token в заголовке `Authorization`

**Пример запроса:**
```bash
curl -X POST "http://localhost:8000/recognize-food" \
  -H "Authorization: Bearer your-token" \
  -F "file=@photo.jpg"
```

**Ответ:**
```json
{
  "recognized_foods": [
    {
      "name": "pizza",
      "confidence": 0.95,
      "nutrition_per_100g": {
        "calories": 266.0,
        "proteins": 12.0,
        "fats": 10.0,
        "carbs": 33.0
      },
      "portion_estimate": {
        "weight_g": 200.0,
        "volume_ml": 250.0,
        "estimated_portion_size": "medium"
      },
      "total_nutrition": {
        "calories": 532.0,
        "proteins": 24.0,
        "fats": 20.0,
        "carbs": 66.0
      }
    }
  ],
  "message": "Распознано: pizza (уверенность: 95.0%)",
  "processing_time_seconds": 2.5
}
```

### GET /health

Проверка работоспособности API.

### GET /docs

Интерактивная документация API (Swagger UI).

### GET /requests

Получить список всех запросов (требует токен).

### POST /train-model

Запустить обучение модели (требует токен).

## Конфигурация

Все настройки в файле `.env`:

- `API_TOKEN` - секретный токен для аутентификации
- `DATABASE_URL` - строка подключения к PostgreSQL
- `OPENAI_API_KEY` - ключ для ChatGPT (опционально)
- `OPENAI_PROXY` - прокси для доступа к OpenAI API (опционально, для обхода блокировок)
- `USE_TRAINED_MODEL` - использовать дообученную модель
- `USE_CHATGPT` - включать ChatGPT для сбора данных
- `USE_OWN_MODEL_INSTEAD_CHATGPT` - возвращать результаты своей модели вместо ChatGPT

Подробнее в `.env.example`.

## Архитектура

1. **Распознавание блюда**: Модель Food-101 определяет тип еды
2. **Оценка калорий**: Гибридный подход - прямое оценивание + база данных
3. **Оценка порции**: Анализ размера на изображении через OpenCV
4. **Сохранение данных**: Все запросы сохраняются в PostgreSQL
5. **Обучение модели**: Данные ChatGPT используются для дообучения

## Разработка

### Структура проекта

```
app/
├── main.py              # FastAPI приложение
├── models.py            # SQLAlchemy модели
├── schemas.py           # Pydantic схемы
├── ml_service.py        # ML сервис
├── chatgpt_service.py   # ChatGPT интеграция
├── training/            # Модули обучения
└── ...
```

### Запуск тестов

```bash
# Добавьте тесты в проект
pytest
```

## Лицензия

MIT

## Автор

Ваше имя
