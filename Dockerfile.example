# Пример Dockerfile для запуска приложения в Docker
# Текущая конфигурация использует локальный запуск
# Этот файл сохранен как пример на случай если понадобится Docker-развертывание

FROM python:3.11-slim

WORKDIR /app

# Установка системных зависимостей
RUN apt-get update && apt-get install -y \
    libpq-dev \
    gcc \
    g++ \
    libgl1 \
    libglib2.0-0 \
    libsm6 \
    libxext6 \
    libxrender-dev \
    && rm -rf /var/lib/apt/lists/*

# Копирование requirements
COPY requirements.txt .

# Установка зависимостей
RUN pip install --no-cache-dir -r requirements.txt

# Копирование приложения
COPY ./app /app/app

# Копирование Alembic
COPY ./alembic /app/alembic
COPY ./alembic.ini /app/alembic.ini

# Создание директории для загрузок изображений
RUN mkdir -p /app/uploads/images

# Переменные окружения
ENV PYTHONUNBUFFERED=1
ENV PYTHONDONTWRITEBYTECODE=1

EXPOSE 8000

# Команда запуска
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]

